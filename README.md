# salved-tasks

#### Can we divide it?;
function isDivideBy(number, a, b) {
 return number % a === 0 && number % b === 0;
}

### Power of two;
```javascript
function isPowerOfTwo(n){
  return !(n & (n - 1)) && n != 0;
}
```
   